package com.springboot12;  // This must match the expected package

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class Springboot12SecurityApplication {  // Class name must match filename
    public static void main(String[] args) {
        SpringApplication.run(Springboot12SecurityApplication.class, args);
    }
}